---
description: testA 自动化生成约定总览（.cursor/rules 分包索引）
globs:
  - "tests/**/*.py"
  - "pages/**/*.py"
  - "tests/**/*.yaml"
  - "data_types/**/*.py"
alwaysApply: true
---

# 自动化用例生成约定（testA）— 总览

生成或扩展 UI 自动化时，按下列文档分角色阅读；**目录与命名以本文件为纲**，细则见各子文件。

| 文档 | 内容 |
|------|------|
| [project-structure.mdc](project-structure.mdc) | 仓库目录、技术栈、新代码放置摘要 |
| [pageobject-conventions.mdc](pageobject-conventions.mdc) | Page + elements YAML、`BasePage`、分层与等待 |
| [data-model-conventions.mdc](data-model-conventions.mdc) | `data_types` dataclass 与 YAML 字段对齐 |
| [test-data-conventions.mdc](test-data-conventions.mdc) | 每功能 `tests/data/{feature}_data.yaml`（执行 + 断言数据合一） |
| [test-suite-conventions.mdc](test-suite-conventions.mdc) | `tests/cases`、`page`/`assertion`、参数化模板 |
| [test-steps-conventions.mdc](test-steps-conventions.mdc) | 可选 `tests/plans` 步骤 YAML（设计/对照用） |

## 强制顺序（新功能）

1. `pages/elements/{page}_elements.yaml`
2. `pages/methods/{page}_page.py`（继承 `pages/base_page.py`，`get_locator` 取元素）
3. `data_types/`（若参数化）
4. `tests/data/{feature}_data.yaml`（与 `test_{feature}.py` 同名；单文件含 cases 与断言预期）
5. `tests/cases/test_*.py`

## 规则位置

约定文档**仅**维护在 **`.cursor/rules/`**（本文件为总索引）；与 `README.md` 冲突时以仓库实际代码与 README 为准。
