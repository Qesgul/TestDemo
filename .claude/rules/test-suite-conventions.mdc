---
globs: tests/cases/**/*.py
description: testA 测试套件与用例编写约定（pytest + Playwright + test_base + 诊断断言）
---

# 测试套件约定（testA）

## 核心架构

- **用例位置**：`tests/cases/`，文件名 `test_{feature}.py`，类名 `Test*`，方法名 `test_*`（与 `pytest.ini` 一致）。
- **浏览器页**：统一使用 pytest-playwright 的 **`page` fixture**，构造 Page 时**必须传入** `LoginPage(page)` 等，避免与 `BrowserManager.get_default_page()` 双轨。
- **断言**：优先使用 `conftest.py` 提供的 **`assertion` fixture**（`common/assertions.py` 的 `DiagnosticAssertion`），失败可落盘诊断产物。
- **数据驱动**：参数化数据用 `tests/steps/test_base.py` 中的 `load_typed_cases_from_yaml`、`case_ids`，数据类型在 `data_types/`。

## pytest 标记（与 `pytest.ini` 对齐）

| 标记 | 含义 |
|------|------|
| `quick` | 快速检查 |
| `smoke` | 冒烟 |
| `core` | 核心功能 |
| `main` | 主流程 |
| `popup` | 弹框相关 |
| `ui` | 界面元素 |
| `flaky` | 不稳定用例（配合重试策略） |
| `no_diagnostics` | 禁用诊断捕获 |

示例：

```python
import pytest

@pytest.mark.smoke
@pytest.mark.ui
def test_example(page, assertion):
    ...
```

## Fixtures 约定

- **`page`**：当前用例的 Playwright `Page`，不要自行 `sync_playwright()` 除非封装在 `common/` 且可复用。
- **`assertion`**：诊断断言对象，常用：`assertion.assert_true(cond, message)`、`assertion.assert_equal(a, b, message)` 等。
- **`logged_in_page`**：已登录态 page（需登录用例统一用它，见规则 4）。
- **`mysql_db` / `redis_db`**：数据库 / Redis 薄客户端（查库核对、造数清理、缓存清除，见规则 5）。
- **`api_client`**：独立 API 客户端，按需带 cookie 登录态，**不启动浏览器**（纯接口测试，见规则 5）。
- **`logged_in_api_client`**：复用浏览器登录 session 的 API 客户端（UI+API 混合用例，见规则 5）。
- **`api_assert`**：API 响应诊断断言（`status_ok` / `json_path` 等，失败落同一套诊断报告，见规则 5）。
- 会话级浏览器回收见 **`conftest.py`** 中 `browser_lifecycle`（`BrowserManager.shutdown()`）。

## 参数化用例模板

```python
import pytest
from data_types.test_data_types import LoginCaseData
from pages.methods.login_page import LoginPage
from tests.steps.test_base import load_typed_cases_from_yaml, case_ids

CASES = load_typed_cases_from_yaml("tests/data/login_data.yaml", LoginCaseData)
CASE_IDS = case_ids(CASES)

if not CASES:
    pytest.skip("未匹配到可执行数据", allow_module_level=True)


class TestLogin:
    @pytest.mark.parametrize("case_data", CASES, ids=CASE_IDS)
    @pytest.mark.core
    def test_login_success(self, case_data, page, assertion):
        login_page = LoginPage(page)
        login_page.goto_login_page()
        login_page.login_with(case_data.username, case_data.password)
        assertion.assert_true(
            login_page.page.url != "https://www.znzmo.com/?from=personalCenter",
            message="登录后 URL 未发生预期变化",
        )
```

## 非参数化流程用例

- 预期、账号等与 **`tests/cases/test_{feature}.py`** 同名的 **`tests/data/{feature}_data.yaml`** 中维护；用例内 `load_yaml` / `load_typed_cases_from_yaml` 只读该文件，避免魔法字符串。

## 执行入口

- **本地 / CI 直接 pytest**：遵循 `pytest.ini` 的 `testpaths = tests/cases`。
- **分组 / 标签批量跑**：`run_suite.py` + `test_suite.yaml`（与 `config/settings.yaml` 中的执行配置配合）。

## 禁止

- 在用例文件中硬编码大量选择器（应下沉到 `pages/elements/*.yaml`）。
- 在用例中写与 `data_types` 字段不一致的 YAML 结构（会导致 `load_typed_cases_from_yaml` 构造失败）。

---

## Playwright 用例代码红线（规则 6）

以下红线适用于 `tests/cases/**/*.py` 中所有 UI 自动化用例，新增或修改用例时必须逐条自检。

### 1. 禁止硬等待

禁止用固定时间等待掩盖时序问题：

```python
# ❌ 禁止：不可解释且容易在 CI 抖动
page.wait_for_timeout(3000)
time.sleep(3)
```

优先使用 Playwright 自动等待、可重试断言、业务状态等待或接口等待：

```python
# ✅ 等业务结果
expect(page.get_by_text("提交成功")).to_be_visible()

# ✅ 等 loading 消失
expect(page.locator(".loading")).to_be_hidden()

# ✅ 等接口响应
with page.expect_response(lambda r: "/api/search" in r.url and r.status == 200):
    page.get_by_role("button", name="搜索").click()
```

确有动画缓冲、第三方 SDK 初始化等不可替代场景时，必须写注释说明原因，并优先封装在公共等待工具中。

### 2. 禁止空断言和伪造通过

禁止为了让用例通过写无意义断言：

```python
# ❌ 禁止
assert True
assertion.assert_true(True, name="占位通过")
```

必须断真实业务结果：

```python
# ✅ 推荐
assertion.assert_true(
    page.get_by_text("知币不足").is_visible(),
    name="知币不足提示展示",
)
```

若功能未实现、埋点未触发、接口无返回或需特定环境，按项目缺陷反馈 / pending 规则处理，不伪造断言。

### 3. 断言验证业务结果，不只验证点击过程

点击、输入、悬浮等动作本身不是测试结论。每个用例至少要有一个可验证的业务结果断言：

```python
# ❌ 弱：只证明执行了点击
page.get_by_role("button", name="提交").click()

# ✅ 强：证明业务完成
page.get_by_role("button", name="提交").click()
expect(page.get_by_text("提交成功")).to_be_visible()
```

优先断 UI 状态、URL、弹窗、接口响应、数据变化、埋点上报等真实结果。

### 4. 用例必须独立

禁止让用例依赖执行顺序或依赖上一条用例留下的状态：

```text
❌ test_01_login -> test_02_search_after_login -> test_03_download_after_search
```

每条用例应通过 fixture、测试数据、接口造数或页面初始化自行准备前置条件。需要登录态统一使用 `logged_in_page`，不要在用例内部临时重复登录。

### 5. Fixture 管环境，测试函数管业务

浏览器上下文、登录态、账号池、页面初始化、接口 mock、数据清理等环境能力应放在 fixture 或公共工具中；测试函数只表达业务流程和断言。

推荐：

```python
def test_vip_model_download(logged_in_page, assertion):
    page_obj = ModelDetailPage(logged_in_page)
    page_obj.open_vip_model()
    page_obj.click_place_model()
    assertion.assert_true(page_obj.is_download_success_visible(), name="下载成功弹窗展示")
```

避免在测试函数里堆叠大量环境准备、账号读取、cookie 注入、临时 selector 和清理逻辑。

### 6. 弹窗、新页面、接口必须使用专用等待

新开页面：

```python
with page.expect_popup() as popup_info:
    page.get_by_text("AI绘图VIP").click()
popup = popup_info.value
expect(popup).to_have_url(re.compile("ai.znzmo.cn"))
```

弹窗：

```python
modal = page.locator(".ant-modal")
expect(modal).to_be_visible()
expect(modal.get_by_text("充值")).to_be_visible()
```

接口：

```python
with page.expect_response(lambda r: "/api/order" in r.url and r.status == 200):
    page.get_by_role("button", name="支付").click()
```

禁止点击后立即读取状态而不等待目标页面、弹窗或接口完成。

### 7. 测试数据不得散落硬编码

账号、商品 ID、搜索词、权益状态、价格、URL 预期、埋点字段等数据应维护在 `tests/data/{feature}_data.yaml` 或对应 dataclass 中。

用例里允许出现少量稳定 UI 文案断言；大量业务数据、账号密码、可变 ID、金额、版本号禁止直接写死。

### 8. 用例命名表达业务语义

测试方法名应说明业务条件和预期结果：

```python
# ✅ 推荐
def test_non_vip_download_vip_model_shows_payment_modal(...):
    ...

# ❌ 禁止
def test_click_button_01(...):
    ...
```

命名优先包含角色/状态、动作、期望结果，方便失败后直接定位业务场景。

---

## 动态列表数据校验策略（规则 2）

对于**列表类**或**多字段强内容**的动态数据（排行榜、模型列表、搜索结果等）：

### 禁止

```python
# ❌ 裸 len 断言：不能说明数据内容是否正确
assert len(items) >= 1

# ❌ 固定 max_items 截断：掩盖条数不足的问题
items = page.get_items_texts(max_items=5)
```

### 必须使用快照比对三件套

```python
from common.snapshot_compare import (
    load_snapshot_from_yaml,
    compare_and_print,
    save_snapshot_to_yaml,
    assert_not_empty,
)

# 用例开始时加载上次快照
prev = load_snapshot_from_yaml("tests/data/{feature}_data.yaml")

# 采集数据（max_items=0 全取）
items = page_obj.get_xxx_items_texts()

# 差量比对（打印新增/减少/不变）
compare_and_print("key_name", prev.get("key_name", []), items)

# 存在性断言（结合历史快照给出有意义错误信息）
assert_not_empty("功能名称", items, prev.get("key_name", []), assertion)

# 用例结束统一写回快照
save_snapshot_to_yaml("tests/data/{feature}_data.yaml", current_data)
```

### 快照存储约定

- 快照写入**对应的数据 YAML 文件**（`tests/data/{feature}_data.yaml`）的 `snapshots:` 键
- **不单独创建 JSON 文件**（禁止 `tests/data/snapshots/*.json`）
- 快照数据保留卡片**所有有效行**，用 ` | ` 拼接（序号 | 名称 | 下载量 | 收益区间），不只取首行

```yaml
# tests/data/create_inspiration_flow_data.yaml 示例
snapshots:
  captured_at: '2026-05-26T09:39:46'
  rank_3d:
    - 法式复古客餐厅 | 109216 | 172-222元
    - 客厅主灯 | 36185 | 120-170元
```

### 数据采集脚本规范

- 凡需要建立/更新快照基准，需编写独立的 `scripts/collect_{feature}_data.py`
- 脚本**先采集打印，不立即写文件**；等人工确认数据合理后再写入（配合 `.claude/AGENTS.md` 规则 1）

---

## 登录态用例统一走 logged_in_page（规则 4）

任何需登录态的用例（埋点、福利领取、下单、个人中心、登录才显示的元素等），
fixture 用 `logged_in_page` 替代 `page`，不在用例内临时登录，也不因"需登录"就 skip：

```python
def test_xxx(self, logged_in_page, assertion):   # ✅ 而非 page
```

- **会话级**：底层 `login_with(use_cookie=True)`，cookie 优先 + 密码兜底，整个 session 只登一次、复用同一 context，无需自己 `add_cookies` / 塞 `storage_state`。
- **自动接线**：`assertion` / `gio_tracking` 等 fixture 走 `_resolve_active_page`，`logged_in_page` 优先自动选中，无需额外接线。
- **区分两类"需登录"**，别一律 skip：
  - 仅需登录态 → `logged_in_page` 直接实装；
  - 还需接口返回特定结果（成功/失败分支）→ 标 pending + `page.route()` 打桩。
- **副作用提醒**：登录态触发"领取/下单/扣费/删除"会对真账号产生真实副作用，docstring 注明，优先接口桩而非真实操作。

---

## 断言与可自动化实战教训（规则 3）

### 1. `assertion` 必带 `name=` 且先确认签名

`assert_equal` 完整签名：`assert_equal(actual, expected, message="", name=None)`。
**第 3 个位置参数是 `message` 而非 `name`**——写成 `assert_equal(a, b, "校验名")` 会被吞进
`message`，校验点汇总表中该行丢失名称。比对断言**必须用关键字**：

```python
# ❌ 错误：第三位置参数被当 message，name 丢失
assertion.assert_equal(actual, expected, "首屏文案")

# ✅ 正确：显式关键字传 name
assertion.assert_equal(actual, expected, name="首屏文案")
```

同理，`assert_true(value, message="", name=None)` 的第 2 位是 `message`；两个方法写 `name` 时
**一律用关键字参数**，不依赖位置。

### 2. headless 不支持 `:hover`，视觉类留 headed

headless Chromium 不触发 CSS `:hover` 伪类，hover 放大 / 高亮 / 动画类断言在 CI 中不可靠。
此类用例标 `manual` 或在注释注明 `# headed-only`，不伪造通过。
典型如卡片 hover 放大 / 橙色高亮，归 manual skip，**禁止写 `assert_true(True)` 假通过**。

### 3. 真实动态数据用软校验

依赖后端实时返回的动态数据（区间数字、排行榜、评论条数等），冷启动或 headless 模式可能返回 0
或极小值。应使用区间 / `>=` / 存在性软校验，数据缺失时 `pytest.skip`，禁止写死等值断言。
写法：文案格式（如 `"近7天已帮助…位设计师"`）用硬断言；其中的动态数值用区间软校验，数值缺失则 skip。

---

## 数据库 / Redis / API 能力使用约定（规则 5）

UI 之外，用例可直接访问**数据库、Redis、HTTP 接口**做核对 / 造数 / 接口测试。能力全部以
fixture 注入，配置统一走 `config/settings.yaml` + 环境变量覆盖，**禁止在用例 / Page 里硬编码连接串或 base_url**。

### ① 数据库查改（`mysql_db`）

后端状态核对（"列表新增一条"、"状态变更为已支付"等）用查库代替纯 UI 猜测；造数 / 清理用写库。

```python
# 查询：参数化 %s 占位防注入；默认 DictCursor，行即 dict
with mysql_db.connection() as conn:
    with conn.cursor() as cur:
        cur.execute("SELECT radio FROM user_group_common WHERE group_name=%s", ["xxx"])
        row = cur.fetchone()
assertion.assert_equal(row["radio"], "0.5", name="切量值落库正确")

# 写入：必须显式 commit=True 才落库（默认不提交，防误写生产）
with mysql_db.connection(commit=True) as conn:
    with conn.cursor() as cur:
        cur.execute("UPDATE user_group_common SET radio=%s WHERE group_name=%s", [r, g])
```

- **参数化查询**：一律 `%s` + 参数列表，**禁止** f-string 拼 SQL。
- **写库副作用**：操作的是真实库，写库用例 docstring 注明，必要时 teardown 复原。

### ② Redis（`redis_db`）

```python
with redis_db.client() as r:
    r.delete("znzmo:group:all")          # 清缓存
    assert r.exists("some:key") == 1     # 命中校验
```

### ③ API 接口测试（`api_client` / `api_assert`）

接口断言走 `api_assert`（**非裸 `assert`**），失败与 UI 用例落**同一套关键校验点汇总 + 诊断报告**。

```python
def test_group_list(self, api_client, api_assert):       # 纯接口用例：不声明 page，不启动浏览器
    resp = api_client.get("/api/group/list", params={"type": "all"})
    api_assert.status_ok(resp, name="分组列表接口可用")        # 断 2xx
    api_assert.json_path(resp, "code", 0, name="业务码为0")    # 点路径取值断言
    api_assert.json_path(resp, "data.0.group_name", "默认分组", name="首个分组名")
```

- `api_client` 方法：`get/post/put/delete(path, params=..., json=..., headers=...)`，返回 `ApiResponse`
  （`.status` / `.ok` / `.json()` / `.text` / `.elapsed_ms`）；`path` 自动拼 base_url，写绝对 URL 也支持。
- `api_assert` 方法：`status_ok` / `status_is(code)` / `json_path(path, expected)` / `json_contains(path, member)`，
  **每个都带 `name=`**（同 UI 断言规范）。
- **写接口**（POST/PUT/DELETE 改后端）有真实副作用，示例 / 未确认接口默认 `pytest.skip`，不擅自打生产。

### ④ 两类 API session（按场景选 fixture）

| fixture | 登录态来源 | 是否开浏览器 | 适用 |
|---|---|---|---|
| `api_client` | cookie 文件载入 storage_state（有则带，无则匿名） | **否**（最快） | 纯接口测试 / 造数 |
| `logged_in_api_client` | 复用 `logged_in_context`（浏览器登录 session） | 是 | UI+API 混合用例 / 无 cookie 冷启动兜底 |

### 红线

- **不硬编码凭据 / 连接串 / base_url**：全部 `config/settings.yaml` + 环境变量（`TEST_DB_*` / `TEST_REDIS_*` / `TEST_API_BASE_URL`）。
- **API 断言用 `api_assert`**，不用裸 `assert` / `playwright.expect`（否则失败不落诊断）。
- **纯接口用例不声明 `page` / `logged_in_page`**，避免无谓拉起浏览器。
- **写库 / 写接口**区分真实副作用，参照规则 4 副作用提醒，docstring 注明。
