# test-design 工作流约束

本文档约束 test-design agent 的执行流程，确保评审意见被正确应用到最终用例。

---

## 核心原则

**评审不是终点，修改才是。** 评审发现的问题必须在最终用例中体现修改。

---

## 标准工作流（必须按顺序执行）

```
┌─────────────────────────────────────────────────────────────┐
│  Step 1: 生成初版测试用例                                      │
│  输出: test-cases-v1.md                                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: 评审初版用例                                          │
│  输出: test-review-report.md                                  │
│  内容: 发现的问题、改进建议、风险点                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 3: 根据评审意见修改用例  ← 这步不能跳过！                   │
│  输出: test-cases-final.md                                    │
│  要求: 每条评审意见都要有对应的修改                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: 基于最终版本生成执行文件                                │
│  输出: test-cases.xlsx（调用 utils/testcase_to_xlsx.py）       │
│  要求: 必须基于 test-cases-final.md，不是 v1                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 5: 生成测试报告                                          │
│  输出: test-report.md                                         │
│  内容: 评审修改说明、覆盖率分析、风险评估                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 文件命名规范

| 阶段 | 文件名 | 说明 |
|------|--------|------|
| 初版用例 | `test-cases-v1.md` | 评审前的原始版本 |
| 评审报告 | `test-review-report.md` | 评审发现的问题和建议 |
| 最终用例 | `test-cases-final.md` | 评审修改后的版本 |
| 执行文件 | `test-cases.xlsx` | 基于 final 版本生成 |
| 测试报告 | `test-report.md` | 包含评审修改说明 |

---

## 评审修改要求

### 1. 修改必须可追溯

在 `test-cases-final.md` 中，每条修改都要标注来源：

```markdown
<!-- 评审意见 #3: 补充数据持久化验证 -->
| TC-TMPL-004A | 验证收藏后刷新页面数据持久 | P1 | ... |
```

### 2. 评审报告必须包含修改清单

`test-review-report.md` 末尾必须有「修改清单」章节：

```markdown
## 修改清单

| # | 评审意见 | 修改内容 | 新增/修改用例 |
|---|---------|---------|--------------|
| 1 | 搜索评分缺少参数化 | 拆分为具体用例 | TC-SEARCH-016A/B/C |
| 2 | 降级逻辑描述模糊 | 明确具体行为 | TC-BUBBLE-013 |
```

### 3. 最终版本必须包含所有修改

`test-cases-final.md` 必须：
- 包含 v1 的所有用例（保留）
- 包含评审新增的用例
- 包含评审要求修改的用例（原用例标注已修改）

---

## xlsx 生成规范

### 必须使用统一工具

```python
# 方式 1: Python 调用
from utils.testcase_to_xlsx import convert_md_to_xlsx
convert_md_to_xlsx("tests/data/test-cases-final.md")

# 方式 2: 命令行
python utils/testcase_to_xlsx.py tests/data/test-cases-final.md
```

### 输入格式要求

Markdown 表格必须包含以下列（按顺序）：

| 列名 | 必填 | 说明 |
|------|------|------|
| 用例编号 | ✅ | 格式: TC-[模块]-[序号] |
| 用例标题 | ✅ | 简洁描述测试目标 |
| 优先级 | ✅ | P0 / P1 / P2 |
| 前置条件 | ✅ | 执行前的环境/数据准备 |
| 测试步骤 | ✅ | 具体操作步骤 |
| 预期结果 | ✅ | 可验证的预期行为 |

### 兼容字段名

以下字段名会自动映射：
- `用例ID` → `用例编号`
- `操作步骤` → `测试步骤`
- `步骤` → `测试步骤`
- `预期` → `预期结果`
- `结果` → `预期结果`

---

## 质量检查清单

执行完成后，自查以下项目：

- [ ] 初版用例是否完整生成？
- [ ] 评审报告是否列出具体问题？
- [ ] 最终用例是否包含评审意见的修改？
- [ ] xlsx 是否基于 final 版本生成（不是 v1）？
- [ ] 测试报告是否说明了评审修改内容？
- [ ] 用例数量是否一致（final vs xlsx）？

---

## 常见错误

### ❌ 错误 1: 评审后未修改用例

```
生成 v1 → 评审 → 直接用 v1 生成 xlsx
```

**问题**: 评审意见没有体现到最终用例中。

### ❌ 错误 2: xlsx 基于错误版本

```
生成 v1 → 评审 → 生成 final → 但 xlsx 基于 v1
```

**问题**: xlsx 和 final 版本不一致。

### ✅ 正确流程

```
生成 v1 → 评审 → 根据意见修改生成 final → 基于 final 生成 xlsx
```
